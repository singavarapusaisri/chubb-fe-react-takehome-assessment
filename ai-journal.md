# AI Journal - Chubb APAC Policy Dashboard Development

| Prompt Given | Result | Status |
| :--- | :--- | :--- |
| "Initialize the project by strictly adhering to the provided `requirements.md`, `tech-stack.md`, and `rules/` documentation. Ensure all subsequent code generation and architectural decisions are guided by these foundational documents." | Acknowledged and confirmed adherence to all specified project guidelines and constraints for the entire development lifecycle. | **Accepted** |
| "Scaffold a Vite React project with MUI v5, Tailwind CSS, and TanStack Query. Set up a directory structure following a feature-based architecture (e.g., src/features/policies) as defined in architecture.md." | Project boilerplate with initial folder structure, Tailwind configuration, and MUI base theme. | **Accepted** |
| "Write a Node.js script to seed `db.json` for `json-server`. It must generate 200+ records conforming to the APAC schema in requirements.md using `faker`." | Generated a script that produced 100 entries with generic North American names and regions like 'California'. | **Rejected** |
| "The seed data lacks APAC regionality and record count. Refactor the script to use specific locales for policyholder names (e.g., Japanese, Singaporean) and strictly enforce the 200-record minimum with the defined APAC regions." | Refined script using `faker-js/faker` with localized name providers and correct region/currency mapping. | **Accepted** |
| "Implement a `useLocalStorage` custom hook and a `useTheme` hook. It must respect `prefers-color-scheme` but allow manual overrides. Encapsulate all storage logic to avoid direct `localStorage` calls in components." | Custom hooks created with persistence logic and system preference detection. | **Accepted** |
| "Create a `usePolicyFilters` hook using `react-router-dom`'s `useSearchParams`. Synchronize search terms, status, LoB, region, and pagination state with the URL. Ensure the state is shareable." | Hook implemented. Correctly parses URL and provides update functions. | **Accepted** |
| "Design the MUI Theme with custom tokens for Light and Dark modes. Ensure the color contrast meets WCAG 2.1 AA standards for all status chips (Active, Expired, etc.)." | Theme object defined with palette overrides. | **Accepted** |
| "Implement robust error handling for API calls within `policyApi.js`, including custom error types for network issues and server responses. Ensure `usePolicies` can gracefully handle and expose these errors." | Basic `try-catch` with generic error messages. Did not differentiate between network and API-specific errors, and lacked structured error objects. | **Rejected** |
| "Refactor `policyApi.js` error handling to introduce specific `NetworkError` and `ApiError` classes. Ensure `usePolicies` maps these to user-friendly messages and provides a retry mechanism." | API service now uses custom error classes, and `usePolicies` correctly surfaces distinct error states. | **Accepted** |
| "Build the `PolicyTable` component using MUI Table. Implement multi-select checkboxes. For the 'Flag for Review' action, use a basic React Query mutation." | Table UI provided with functional checkboxes, but inputs lacked unique `aria-label` attributes and keyboard navigation was ignored. | **Challenged** |
| "Update the `PolicyTable` to include descriptive `aria-label` attributes for checkboxes (e.g., 'select policy POL-123') and ensure the 'Select All' toggle is accessible via keyboard." | Updated component with full ARIA compliance and focus management. | **Accepted** |
| "Implement the `useFlagForReview` mutation to update the `flaggedForReview` status. Trigger a cache invalidation on success." | Implementation used a simple `onSuccess` refetch, causing a noticeable UI lag for bulk actions. | **Rejected** |
| "Refactor `useFlagForReview` to implement Optimistic UI updates. Use `onMutate` to snapshot the cache and perform a manual update to the list, with an `onError` rollback mechanism." | Optimized mutation logic providing instantaneous feedback and robust error recovery. | **Accepted** |
| "Create a `FilterBar` component. Include a global search input and dropdowns for Status and Region. Add a 'Collapse' section for advanced Date Range filters (Effective and Expiry)." | Composed FilterBar with MUI Grid and Collapse. | **Accepted** |
| "Implement the `StatsPanel` with dynamic cards. Calculate total premium volume for 'Property' and 'Casualty' specifically, and count policies expiring in the next 30 days." | Metrics calculated correctly in the hook, but the UI applied the division by 1,000,000 only to the Casualty line, showing incorrect totals ($120000080.0M). | **Challenged** |
| "Fix the premium aggregation logic in `StatsPanel`. Both lines must be summed before dividing by the million factor to ensure correct formatting." | Corrected formatting logic displaying clean currency strings (e.g., $200.0M). | **Accepted** |
| "Configure Vitest and React Testing Library. Generate comprehensive test cases for the Table, FilterBar, and custom hooks. Mock the API responses using Vitest spies." | Test suites covering loading, empty, and error states. | **Accepted** |
| "Optimize the `PolicyTableMUI` component for performance, specifically focusing on preventing unnecessary re-renders during filter changes and pagination. Leverage `React.memo` and `useCallback` where appropriate." | Applied `React.memo` to the `PolicyTable` component, but missed memoizing callback functions passed down to child components, leading to continued re-renders. | **Challenged** |
| "Refine `PolicyTableMUI` performance by applying `useCallback` to all event handlers and `useMemo` to derived values passed as props, ensuring `React.memo` on child components is effective." | Achieved significant reduction in re-renders through targeted memoization of callbacks and derived values. | **Accepted** |
| "Perform a final accessibility audit. Ensure keyboard navigation works for pagination and that 'Flag for Review' has a loading state during execution." | Refined component with better focus management and ARIA live regions. | **Accepted** |